stages:
  - build
  - test

build:
  stage: build
  script:
    - cd "REST API"
    - ./gradlew sonarqube
    - ./gradlew build -x test
    - ./gradlew.bat --stop

test:
  stage: test
  script:
    - cd "REST API"
    - ./gradlew sonarqube
    - ./gradlew test
    - ./gradlew.bat --stop
