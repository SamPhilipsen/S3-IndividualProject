stages:
  - build
  - test

build:
  stage: build
  script:
    - cd Code
    - ./gradlew build -x test
    - ./gradlew.bat --stop

test:
  stage: test
  script:
    - cd Code
    - ./gradlew test
    - ./gradlew.bat --stop
